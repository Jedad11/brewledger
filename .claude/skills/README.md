# Project skills

Placeholder — no custom skills exist yet. When adding one, use the standard
Claude Code layout:

```
.claude/skills/<skill-name>/SKILL.md
```

Natural candidates from this repo's own workflows, if/when they're worth
automating: running `db:migrate` + `db:seed` together, starting both dev
servers (`apps/console` + `apps/api`) at once, or running the RL-3
`lint:boundary` check. None of these are built — this file just records the
convention.
