# Copy to .env (and to packages/db/.env, apps/api/.env — see README) once a
# Postgres instance is available, then run:
#   pnpm db:migrate && pnpm db:seed
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/brewledger?schema=public"
PORT=3001
