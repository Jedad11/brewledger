# Copy to packages/db/.env for local Prisma CLI usage (migrate/seed/studio).
# apps/api reads DATABASE_URL from its own .env — this file only affects the
# Prisma CLI when run from within packages/db.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/brewledger?schema=public"
