{
  "name": "@brewledger/db",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "typesVersions": {
    "*": {
      "types": ["src/types.ts"]
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p tsconfig.tests.json",
    "lint": "echo \"(no lint configured for @brewledger/db)\"",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2",
    "@types/node": "^22",
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
