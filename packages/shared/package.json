{
  "name": "@brewledger/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured for @brewledger/shared)\"",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2",
    "zod": "^3"
  },
  "devDependencies": {
    "@brewledger/db": "workspace:*",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
