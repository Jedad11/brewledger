{
  "name": "@brewledger/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured for @brewledger/worker)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@brewledger/shared": "workspace:*",
    "@sentry/node": "^8.55.2",
    "@supabase/supabase-js": "^2",
    "pg": "^8.23.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pg": "^8.23.1",
    "@types/web-push": "^3.6.4",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
